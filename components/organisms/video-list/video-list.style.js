import styled from "styled-components";

export const VideoListWrapper = styled.article`
  display: grid;
  grid-template-columns: repeat(4, 290px);
  column-gap: 12px;
  row-gap: 36px;
  margin-top: 48px;
  padding-left: 20px;
`